1. Create a folder "data" in this folder.
In the folder "data" will be created the logs and extraction files.

2. Verify the folder where the postgresql utilities are found. 
The utilities that are used from extraction and consolidation are psql.exe, pg_dump.exe, pg_restore.exe.
